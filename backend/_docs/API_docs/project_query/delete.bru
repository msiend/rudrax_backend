meta {
  name: delete
  type: http
  seq: 5
}

delete {
  url: {{URL.Entity}}query/remove
  body: json
  auth: bearer
}

auth:bearer {
  token: <your_token_here>
}

body:json {
  {
    "id": 2
  }
}

assert {
  ~status: isTrue
}
