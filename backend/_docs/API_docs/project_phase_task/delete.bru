meta {
  name: delete
  type: http
  seq: 5
}

delete {
  url: {{URL.Entity}}project_subphase/remove
  body: json
  auth: bearer
}

auth:bearer {
  token: <your_token_here>
}

body:json {
  {
    "pro_subphase_id": 1
  }
}

assert {
  ~status: isTrue
}
